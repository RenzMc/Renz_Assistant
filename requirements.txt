# Core dependencies - Termux friendly
# NOTE: On Termux, install numpy via: pkg install python-numpy
# then: pip install -r requirements.txt
python-dateutil>=2.8.2

# NLP - lightweight, Termux compatible
nltk>=3.6.0

# Audio TTS
edge-tts>=6.1.0

# AI and API
requests>=2.26.0

# Weather and location
geopy>=2.2.0
beautifulsoup4>=4.10.0

# Offline voice recognition (optional, choose one)
# vosk>=0.3.45          # Offline STT - install separately if needed
# openai-whisper>=20230314  # Heavy but powerful - install separately if needed
